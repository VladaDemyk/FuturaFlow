spring:

  application:
    name: FuturaFlow
  datasource:
    url: jdbc:mysql://avnadmin:AVNS_4LWTufJ3xfyC3Z7dfUE@futuraflow-03-harbar-0dc7.l.aivencloud.com:12189/defaultdb?ssl-mode=REQUIRED
    username: avnadmin
    password: AVNS_4LWTufJ3xfyC3Z7dfUE
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  flyway:
    baseline-on-migrate: true

    servlet:
    multipart:
      # Максимальний розмір одного файлу
      max-file-size: 10MB
      # Максимальний розмір всього тіла запиту
      max-request-size: 10MB


